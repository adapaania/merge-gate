# merge-gate
An agent proposes a merge. A gate decides: let it through, or hold it for a human. This repo builds that decision function and measures which way of writing it actually works — comparing five escalation policies on a controlled, labeled dataset.
