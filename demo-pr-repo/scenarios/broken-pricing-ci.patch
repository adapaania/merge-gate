diff --git a/demo-pr-repo/src/pricing.py b/demo-pr-repo/src/pricing.py
index cedf124..f171c93 100644
--- a/demo-pr-repo/src/pricing.py
+++ b/demo-pr-repo/src/pricing.py
@@ -12,4 +12,4 @@ def discounted_price(amount: Decimal, discount_rate: Decimal) -> Decimal:
         raise ValueError("discount_rate must be between 0 and 0.50")
 
     discounted = amount * (Decimal("1") - discount_rate)
-    return discounted.quantize(Decimal("0.01"), rounding=ROUND_HALF_UP)
+    return discounted.quantize(Decimal("1"), rounding=ROUND_HALF_UP)
