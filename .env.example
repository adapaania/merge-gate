# Optional: only required for live Claude judgments.
ANTHROPIC_API_KEY=

# The default model used by the live judge.
MERGE_GATE_MODEL=claude-haiku-4-5

# Optional for public PRs; required for private PRs. Use read-only permissions.
GITHUB_TOKEN=
